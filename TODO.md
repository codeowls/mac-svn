# Mac SVN 开发待办

更新日期：2026-09-16。

本清单基于当前已实现功能整理，重点是补齐日常操作、增强历史追溯、完善异常恢复。优先级表示建议顺序，不代表功能已经实现或通过验收。

当前功能见 [README.md](README.md)，验证证据与未完成的验收见 [VERIFICATION.md](VERIFICATION.md)。完成任务后勾选对应条目，并在验证记录中补充实际检查结果；引擎测试通过与完整界面验收分别记录。

## 建议开发顺序

1. 优先处理 P0 中影响现有主流程的问题，并补完已有功能的界面验收。
2. 全局忽略列表已完成；下一轮功能开发建议选择：目录忽略操作、还原、历史文件 Diff。
3. 随后完善冲突处理、重命名与删除及目录操作。
4. 再推进多副本体验、大项目性能和分支管理；面向其他用户分发时推进系统集成与发布能力。

## P0：稳定性与异常恢复

- [ ] 补完取消操作、失败后重新打开、切换副本等实际界面验收。
- [ ] 补完验证记录中未完成的差异弹窗、最近副本右键菜单、悬停提示及认证后历史重载验收。
- [ ] 为更新和提交增加实时操作输出，显示进行中、完成、失败与取消状态。
- [ ] 定位验证记录中的 HTTP 检出截断错误 `E120106`，区分服务器、网络与客户端原因，取得证据后再确定修复方案。
- [ ] 完善失败或取消后残留工作副本的状态检查与处理指引。

验收要点：取消后不残留忙碌状态；旧请求不回填其他副本；原始错误与已收到的日志保留；失败不能显示为成功。检出取消不等于回滚，提交结果不明确时应引导核实历史，不自动重复提交。

## P1：常用文件与目录操作

- [x] 增加应用内全局忽略列表，支持编辑、保存、常用规则、沿用系统配置和重启持久化；已受控文件不受影响。
- [x] 增加显示已忽略项的查看开关，并在添加前检查当前状态，避免旧选择绕过忽略规则。
- [x] 完善 SVN 可执行文件设置，支持选择文件、自动检测及 SVN 1.14+ 版本测试。
- [ ] 增加文件／目录的忽略操作，写入 SVN 忽略属性并展示产生的属性变更；与应用内全局忽略配置区分。
- [ ] 增加还原操作，执行前列出目标及将丢弃的未提交修改，明确确认后执行并刷新状态。
- [ ] 增加 SVN 重命名操作，保留历史关联，展示源路径与目标路径。
- [ ] 增加 SVN 删除操作，明确本地文件与待提交删除状态的影响。
- [ ] 完善目录删除、替换和带历史复制的提交支持，明确涉及的子项后再放开当前限制。

验收要点：操作只影响已确认范围；覆盖文件与目录、中文与特殊字符路径、未提交的父目录及操作失败后的状态。目录操作不能隐式提交未确认的子项。

## P1：历史差异与追溯

- [ ] 点击历史记录中的文件，查看该次提交的具体差异。
- [ ] 历史 Diff 明确显示比较的两个版本，正确处理新增、删除、替换及复制来源。
- [ ] 支持加载更早的历史记录，突破当前最近 50 条的展示范围。
- [ ] 增加作者、提交说明和路径筛选，明确筛选作用于已加载记录还是服务端查询范围。
- [ ] 增加单文件历史入口。

验收要点：选中不同提交或文件后，差异及版本信息同步更新；正确区分无文本差异、二进制文件、无权限和读取失败；历史查看不修改工作副本。

## P1：冲突处理闭环

- [ ] 提供冲突详情与相关版本的查看入口。
- [ ] 支持打开外部合并工具，并展示待处理文件。
- [ ] 用户完成处理并确认后，支持标记已解决、重新读取状态及后续提交。
- [ ] 区分文本、属性和树冲突的处理入口与支持范围。
- [ ] 在上述流程成熟后，评估内置三方合并编辑器。

验收要点：未解决的冲突继续阻止提交；打开或关闭合并工具不能直接视为解决成功；标记解决后以重新读取的 SVN 状态为准。

## P2：多副本与登录体验

- [ ] 按工作副本分别保留提交草稿、筛选条件和选择状态，避免切换副本丢失上下文。
- [ ] 增加可选的钥匙串凭据持久化，提供明确的“记住密码”选择。
- [ ] 增加退出账号和清除已保存凭据入口。

验收要点：不同副本的草稿与选择不串用；恢复选择时核对当前状态。凭据继续按仓库隔离，不写入 UserDefaults、日志或进程参数；未选择记住密码时维持会话内保存行为。

## P2：大项目性能

- [ ] 建立大型工作副本、大 Diff 和长时间检出的性能基线，记录耗时、内存及滚动表现。
- [ ] 根据测量结果优化 Diff 解析、行宽计算与渲染。
- [ ] 根据测量结果优化实时日志刷新频率与展示方式，保留可核查的操作输出。
- [ ] 有实际使用需求后增加稀疏检出与深度设置。

验收要点：优化前后使用相同数据比较；界面持续可操作、取消有效；不能为提升性能静默遗漏文件、差异或错误信息。稀疏检出应明确显示实际检出范围。

## 后续：分支管理、系统集成与分发

- [ ] 在冲突及目录操作成熟后，增加分支切换、创建分支／标签与合并能力。
- [ ] 根据实际需求评估文件锁管理和 externals 管理。
- [x] 完成访达集成可行性调研，见 [访达集成方案](FINDER_INTEGRATION.md)；当前仅完成研究，尚未实现扩展。
- [ ] 验证 Finder Sync 最小原型：扩展加载、启用、目录注册及主程序唤起。
- [ ] 接入访达右键 SVN 操作，明确所选项目与整个工作副本的作用范围。
- [ ] 在菜单流程成熟后，增加状态角标、缓存与后台刷新，并验证多扩展共存。
- [ ] 评估内置 SVN 引擎，减少对本机命令行环境的依赖。
- [ ] 面向其他机器分发前，完成 Developer ID 签名、Apple 公证及目标系统兼容性验证。
- [ ] 公开前确定许可证和发布策略；仓库继续保持私有，等待另行明确要求。

## 执行与完成规则

- 每轮围绕选定功能做小范围增量修改，保留其他未提交变更。
- 按任务范围完成必要测试及实际界面验证，再更新本清单和验证记录。
- 本文档仅记录开发建议，不构成生产数据修改、删除文件、浏览器批量操作、提交、推送或部署的授权；执行时遵循项目规则及当次明确授权。
- 功能完成后先保留在本地，不自动提交或推送。
